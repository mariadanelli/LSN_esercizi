#include <cmath>
#include <iostream>
#include <fstream>
#include <string>
#include <vector>

using namespace std;

double error(double, double, int);
double max (double, double);
vector <double> MediaBlocchi (double, double, vector <double>);